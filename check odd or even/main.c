#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num=0;
    printf("enter a number :");
    scanf("%d",&num);
    if(num !odd);
    printf(" this is not an odd number\n");
    return 0;
}
