#include <stdio.h>
#include <stdlib.h>

int main()
{
       	printf("Enter a character");
// Take a character as input & store it in variable c
	c = getchar();
// display the character stored in variable c
	putchar(c);

    return 0;
}
